# ServiceAccount für Pods, die als root oder mit fester UID laufen müssen.
# postgres:17-alpine läuft als UID 70, nginx:alpine als root — beide brauchen anyuid SCC.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: app-sa
  namespace: securetransfer
