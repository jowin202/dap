apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: securetransfer
  labels:
    app: frontend
spec:
  selector:
    app: frontend
  ports:
    - name: https
      port: 443
      targetPort: 443
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
